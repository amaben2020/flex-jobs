import { Loader2 } from "lucide-react";
import React from "react";

const Loading = () => {
  return (
    <Loader2 size={100} className="mx-auto my-auto h-[60vh] animate-spin" />
  );
};

export default Loading;
